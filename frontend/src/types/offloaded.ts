export interface Offloaded {
    id: number;
    draw_id: number;
    master_dealer_id: string;
    page_no: number;
    ticket: string;
    amount: number;
    note?: string;
    created_at: string;
}
