import layoutStyles from '../styles/Layout.module.scss';

export const SettingsPage = () => (
  <div className={layoutStyles.pageContent}>
    <h1 style={{ gridColumn: '1 / -1' }}>Settings</h1>
  </div>
);
